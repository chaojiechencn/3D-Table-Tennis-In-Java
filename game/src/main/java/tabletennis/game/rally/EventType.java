package tabletennis.game.rally;

/** The things that happen to a ball that the rally rules care about. */
public enum EventType {
    RacketHit,
    TableBounce,
    NetTouch,
    FloorTouch,
    OutOfBounds
}
